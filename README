This was just a test to see if PAUSE does anything to prevent
two different tar.gz files by the same name but from different
authors from being uploaded.

For the real version of Test-Verbose, look here instead:

B/BI/BIGJ/Test-Warn-0.07.tar.gz
